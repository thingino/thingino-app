<?xml version="1.0" encoding="utf-8"?>
<!-- The faint accent wash from webflash's .btn-attention, so an outlined
     primary action still carries more weight than an outlined secondary one. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/transparent" android:state_enabled="false" />
    <item android:color="@color/primary_wash" />
</selector>
